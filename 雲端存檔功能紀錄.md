# 風自然優惠券 — 雲端存檔功能紀錄

## 功能目標
- 每次「複製圖片」時，自動把優惠券圖檔上傳到 Firebase Storage
- 每天一張（以日期命名 `coupons/YYYY-MM-DD.png`），同一天重複複製會覆寫
- 一年後再多十天（375 天）自動刪除
- 不需要資料庫

## 目前進度（2026/09/05）
已完成的程式修改：
- ✅ `index.html`：加入 Firebase SDK、匿名登入、`uploadCouponImage()` 上傳邏輯
  （複製/下載流程不變，上傳失敗不影響操作）
- ✅ `storage.rules`：`coupons/` 路徑需登入才可讀寫，其餘路徑全拒絕
- ✅ `firebase.json`：加入 storage rules 設定
- ⬜ GCS Lifecycle（375 天刪除）：設定指令已備妥，待 Storage 啟用後執行

## 🔴 阻礙：需升級 Blaze 才能啟用 Firebase Storage
- 專案 `opencode-sk` 是 **Spark 免費方案，未綁定計費帳戶**
- 2024/10 起 Google 規定新專案要在沒有既有 bucket 的情況下建 Storage，**必須綁定計費（Blaze）**
- 測試過所有替代路線都被擋：defaultBucket create（403）、GCS 建 bucket（計費帳戶未啟用）、App Engine 繞法（需綁計費）
- 這不是容量問題：一年約 200MB，免費額度 5GB，升級 Blaze 也不會超過 $0

## 待辦（升級 Blaze 後）
1. `firebase deploy --only storage` → 建立 default bucket + 套用 security rules
2. `firebase deploy --only hosting` → 上線含上傳功能的網頁
3. 設定 Lifecycle：超過 375 天自動刪除
   ```json
   {
     "lifecycle": {
       "rule": [{ "action": { "type": "Delete" }, "condition": { "age": 375, "isLive": true } }]
     }
   }
   ```
4. push 上 GitHub、更新 `網址.txt` 更新紀錄

## 升級 Blaze 入口
https://console.firebase.google.com/project/opencode-sk/usage/details

## 取消方式
若最後決定不要此功能，把 `index.html` 中 Firebase SDK 三行 script、`firebaseConfig`、`signInAnonymously` 與 `uploadCouponImage()` 移除，刪除 `storage.rules`，並還原 `firebase.json` 即可。